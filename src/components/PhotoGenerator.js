const PhotoGenerator = () => {

    return (
        <div>
            <button>
                Shuffle Photos
            </button>
            <button>
                Generate New Photos
            </button>
        </div>
    );
}

export default PhotoGenerator;